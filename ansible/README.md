Here you place ansible files, playbooks, roles, everything needed for it to work


# Activate virtual environment ( I had to use ansible-community )
``` cd path/to/cloudops-test/ansible ```

``` source .venv/bin/activate ```
