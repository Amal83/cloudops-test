- name: Provision application server
  hosts: all
  become: true
  become_method: sudo

  roles:
    - common
    - ssh
    - firewall
    - journald
    - docker
    - postgres
    - redis
    - app
    - nginx
    - backup
    - systemd
    